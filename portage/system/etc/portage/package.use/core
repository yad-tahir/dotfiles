sys-kernel/gentoo-sources
sys-kernel/genkernel cryptsetup

sys-fs/zfs rootfs

# GCC
sys-devel/gcc graphite -fortran

# @cgroup-hybrid is needed by virt-manager
# Remove @gcrypt as the disk it is already fully encrypted
sys-apps/systemd cgroup-hybrid -elfutils -gcrypt -pcre